(function(window) {
    'use strict'

    /**
     * Core
     */
    const imageTemplate = function() {
        const canvas = {
            /**
             * Init canvas
             */
            init() {

            }
        }
    }

    if(typeof(window.imageTemplate) === 'undefined') {
        window.imageTemplate = imageTemplate();
    }
})(window);